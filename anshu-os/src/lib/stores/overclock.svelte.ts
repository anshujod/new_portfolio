// Overclock mode — unlocked by the Konami code or `sudo overclock` in the terminal.
// The particle field reads this to crank speed, link density and brightness.
export const overclock = $state({ on: false });
